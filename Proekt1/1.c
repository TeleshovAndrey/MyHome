/*#include <stdio.h>
#include <math.h>
int main()
{
	float x1, x2, y1, y2, y, x, z;
	printf("Enter the x and y coordinates of the first point separated by a space \n");
	scanf_s("%f %f", &x1, &y1);
	printf("Enter the x and y coordinates of the second point separated by a space \n");
	scanf_s("%f %f", &x2 ,&y2);
	x = x1 - x2;
	y = y1 - y2;
	z = sqrt(x * x + y * y);
	printf("distance between points %f \n", z);
	return 0;
}*/