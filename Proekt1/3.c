/*#include <stdio.h>
#include <math.h>
int main()
{
	float a, b, c, arif, ge;
	printf("Enter numbers a,b,c separated by spaces respectively \n");
	scanf_s("%f %f %f", &a, &b, &c);
	arif = (a + b + c) / 3;
	ge = pow(a * b * c, 1./ 3);
	if (a !=0 && b !=0 && c!= 0)
		printf(" geometric progression %f", ge);
	else
		printf("Arithmetic progression %f", arif);
	return 0;
}*/